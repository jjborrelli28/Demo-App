import type { NextPage } from "next";
import { Fragment } from "react";
import { Header } from "../components/header";

const Home: NextPage = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default Home;
